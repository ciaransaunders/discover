.PHONY: setup build test clean

setup:
	@echo "✅ Project generated as a Swift Package."
	@echo "Open 'Package.swift' in Xcode by double-clicking it or using 'File > Open'."

build:
	swift build

test:
	swift test

clean:
	swift package clean
	rm -rf .build/
